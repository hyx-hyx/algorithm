#include <iostream>
#include <vector>
#include <algorithm>
#include <string>
using namespace std;
int main()
{
    int n, k;
    cin >> n >> k;
    int w[100], p[100],per[100];
    for (int i = 0; i < n;++i)
    {
        cin >> w[i] >> p[i];
        per[i] = (double)p[i] /(double)w[i];
    }
    for (int i = 0; i < n;++i)
    {
        
    }
        return 0;
}